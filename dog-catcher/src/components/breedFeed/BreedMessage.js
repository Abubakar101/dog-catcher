import React from "react";

const BreedMessage = () => {
  return (
    <div className="emptyMessageContainer">
      <p className="emptyMessage">There are currently no breeds caught.</p>
      <p className="emptyMessage">Search above to catch some!</p>
    </div>
  );
};

export default BreedMessage;
